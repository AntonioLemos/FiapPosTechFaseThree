apiVersion: v1
kind: Pod
metadata:
  name: rabbitmq
  labels:
    app: rabbitmq
spec:
  containers:
    - name: rabbitmq
      image: masstransit/rabbitmq
      ports:
        - containerPort: 15672
          protocol: TCP
        - containerPort: 5672
          protocol: TCP
  restartPolicy: Always
